<form method="post" action="{{ path('participants_groups_delete', {'idParticipantG': participants_group.idParticipantG}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participants_group.idParticipantG) }}">
    <button class="btn">Delete</button>
</form>
