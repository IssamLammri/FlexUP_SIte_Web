<form method="post" action="{{ path('orders_refused_delete', {'idOrderR': orders_refused.idOrderR}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orders_refused.idOrderR) }}">
    <button class="btn">Delete</button>
</form>
