<form method="post" action="{{ path('user_identify_delete', {'id': user_identify.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_identify.id) }}">
    <button class="btn">Delete</button>
</form>
