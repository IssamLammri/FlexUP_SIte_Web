{% extends 'base.html.twig' %}

{% block title %}Currency{% endblock %}

{% block body %}
    <h1>Currency</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCurrency</th>
                <td>{{ currency.idCurrency }}</td>
            </tr>
            <tr>
                <th>NameCurrency</th>
                <td>{{ currency.nameCurrency }}</td>
            </tr>
            <tr>
                <th>Symbol</th>
                <td>{{ currency.symbol }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('currency_index') }}">back to list</a>

    <a href="{{ path('currency_edit', {'idCurrency': currency.idCurrency}) }}">edit</a>

    {{ include('currency/_delete_form.html.twig') }}
{% endblock %}
