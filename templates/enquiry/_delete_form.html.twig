<form method="post" action="{{ path('enquiry_delete', {'idOrder': enquiry.idOrder}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enquiry.idOrder) }}">
    <button class="btn">Delete</button>
</form>
